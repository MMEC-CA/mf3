{
  "name": "beat-cop",
  "version": "1.0.0",
  "private": true,
  "description": "Beat Cop — 2D police simulation game on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx wrangler@4 deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/ _site/"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
